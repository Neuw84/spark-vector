{
  "name": "vecruntime-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A vectorized execution runtime for Apache Spark using Java",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('site',{recursive:true,force:true}))\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "markdown-it": "15.0.2",
    "markdown-it-anchor": "10.0.0"
  }
}
